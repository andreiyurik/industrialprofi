<%# locals: (token:) -%>
<%# The framing circle comes from the context (progress ring, bordered disc, hero circle) — this only centres the icon. %>
<span class="topic-badge">
  <%= icon_tag token %>
</span>
