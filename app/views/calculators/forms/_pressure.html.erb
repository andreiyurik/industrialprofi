<div class="calc__grid">
  <div class="calc__inputs">
    <h2 class="calc__heading"><%= t("calculators.inputs_heading") %></h2>
    <%= render "calculators/field", field: "value", label: t("calculators.fields.value"), value: 1 %>
    <%# Состав и порядок единиц — из той же таблицы множителей, что под калькулятором. %>
    <%= render "calculators/select", field: "unit", label: t("calculators.fields.unit"), selected: "bar",
          options: @calculator.norms[:units].map { [ it[:unit], t("calculators.pressure.units.#{it[:unit]}") ] } %>
  </div>

  <div class="calc__results">
    <h2 class="calc__heading"><%= t("calculators.results_heading") %></h2>
    <% @calculator.norms[:units].each do |unit| %>
      <%= render "calculators/result", output: unit[:unit], label: t("calculators.pressure.units.#{unit[:unit]}") %>
    <% end %>
  </div>
</div>
